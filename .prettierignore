node_modules
dist
build
.turbo
coverage

# Astro's generated content-collection types and cache for docs-site/. Regenerated by every
# `astro dev` / `astro build` / `astro check`, gitignored, and full of .json and .d.ts that the
# root format glob would otherwise sweep up (plans/docs-site/PROPOSAL.md §2.3).
.astro

# The docs-shots sidecars. Both are written by the docs-shots Playwright project
# (`pnpm run docs:shots`), so formatting them here would mean every capture run produced files the
# pre-commit hook immediately rewrote — churn with no reader. Only the generated JSON is ignored;
# the PNGs beside it are not something prettier would touch anyway.
docs-site/src/assets/screenshots/*.json

pnpm-lock.yaml
*.min.js
*.min.css
