# Astro's generated content-collection types and cache. Regenerated by every
# `astro dev` / `astro build`, and never a build input.
.astro/

# Build output. The root .gitignore already globs `dist/`; repeated here so this
# directory stays self-describing if it is ever extracted.
dist/
node_modules/
