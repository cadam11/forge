---
title: Page not found
description: That page does not exist on this site.
template: splash
editUrl: false
---

Nothing lives at that address.

Most of this site is still being written — feature guides, the command and shortcut reference,
and troubleshooting are all in progress — so a link from elsewhere may be pointing at a page
that has not been published yet.

- [Start at the beginning](/getting-started/install/)
- [Search the site](/) — the search box is in the header
- [Report a broken link](https://github.com/cadam11/joinery/issues)

<!--
  This is the one page whose internal links are written root-absolute rather than relative:
  GitHub Pages serves it for a request at ANY depth, so a relative link has no fixed
  meaning here. Since J-108 the site is served from the `usejoinery.com` apex with no
  `base`, so a root-absolute link IS the site root — these two need no prefix. If a `base`
  is ever reintroduced, starlight-links-validator resolves these against it and fails the
  build rather than letting them silently 404. Do not copy this pattern onto any other page.
-->
