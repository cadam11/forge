---
title: Page not found
description: That page does not exist on this site.
template: splash
editUrl: false
---

Nothing lives at that address.

Most of this site is still being written — feature guides, the command and shortcut reference,
and troubleshooting are all in progress — so a link from elsewhere may be pointing at a page
that has not been published yet.

- [Start at the beginning](/joinery/getting-started/install/)
- [Search the site](/joinery/) — the search box is in the header
- [Report a broken link](https://github.com/cadam11/joinery/issues)

<!--
  This is the one page whose internal links are written base-first rather than relative:
  GitHub Pages serves it for a request at ANY depth, so a relative link has no fixed
  meaning here. Both links therefore carry the `/joinery` base explicitly, and
  starlight-links-validator resolves and checks them against `base` — so if `base` ever
  changes (a custom domain, per plans/docs-site/PROPOSAL.md D1) these two links fail the
  build rather than silently 404. Do not copy this pattern onto any other page.
-->
