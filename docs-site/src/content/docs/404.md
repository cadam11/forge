---
title: Page not found
description: That page does not exist on this site.
template: splash
editUrl: false
---

Nothing lives at that address.

A page may have moved, or a link elsewhere may be pointing at an address this site never had.
Search is the fastest way back — it covers every page.

- [Start at the beginning](/getting-started/install/)
- [Troubleshooting](/troubleshooting/) — if you came here looking for a fix
- **Search the site** — the box in the header above covers every page
- [Report a broken link](https://github.com/cadam11/joinery/issues)

<!--
  This is the one page whose internal links are written root-absolute rather than relative:
  GitHub Pages serves it for a request at ANY depth, so a relative link has no fixed
  meaning here. Since J-108 the site is served from the `usejoinery.com` apex with no
  `base`, so a root-absolute link IS the site root — they need no prefix. If a `base`
  is ever reintroduced, starlight-links-validator resolves these against it and fails the
  build rather than letting them silently 404. Do not copy this pattern onto any other page.

  "Search the site" is deliberately NOT a link since J-99 Phase 3. It used to point at `/`, which
  was the Starlight splash and did carry the header search box. `/` is now the landing route
  (`src/pages/index.astro`), a plain Astro page with no Starlight chrome and therefore no search
  box, so the sentence would have been false — and the link would also have failed the build:
  starlight-links-validator classifies a link to a non-Starlight page as
  `InvalidLinkToCustomPage`. The search box on THIS page is the one being described, and it is
  right above.
-->
