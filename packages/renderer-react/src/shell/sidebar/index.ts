/**
 * The sidebar. `Sidebar` is the only export the shell needs; everything else here is internal to
 * the split and is imported by path from within this directory.
 */

export { Sidebar } from './sidebar';
